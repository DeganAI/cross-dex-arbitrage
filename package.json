{
  "name": "cross-dex-arbitrage",
  "version": "1.0.0",
  "description": "Scan for price discrepancies across DEXes via x402",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@lucid-dreams/agent-kit": "^0.2.24",
    "hono": "^4.x",
    "zod": "^3.x"
  },
  "devDependencies": {
    "bun-types": "^1.x"
  }
}
